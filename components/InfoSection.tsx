import styled from 'styled-components';

const InfoSection = styled.div`
    max-width: 768px;
    margin: auto;
    padding: 1rem;

    h2 {
        color: var(--maroon);
    }
`

export { InfoSection }
